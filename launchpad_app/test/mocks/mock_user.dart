import 'package:firebase_auth/firebase_auth.dart';
import 'package:mockito/annotations.dart';

// Generate a MockUser class using the Mockito generator.
// Mocks can be generated using the `dart pub run build_runner build` command.

@GenerateNiceMocks([MockSpec<User>()])
void main() {}
